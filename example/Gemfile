# frozen_string_literal: true

source "https://rubygems.org"

# Build the demo site against the local theme gem. The theme's gemspec declares
# Jekyll and every plugin listed in _config.yml as runtime dependencies, so they
# are pulled in automatically.
gemspec path: ".."

gem "webrick"
